// 77;
// "Harry bhai"
// false
console.log("Operators in JS")
// Arithmetic Operators 
let a = 10; // Assignment Operator
let b = 4; // Assignment Operator
console.log("a + b = ", a + b) // Addition Operator
console.log("a - b = ", a - b) // Subtraction Operator
console.log("a / b = ", a / b) // Division
console.log("a * b = ", a * b) // Multiplication Operator
console.log("a ** b = ", a ** b) // Exponentiation
console.log("a % b = ", a % b) // Modulus (Division Remainder)
console.log("++a = ", ++a) // Increment
console.log("a++ = ", a++)
console.log("--a = ", --a)
console.log("a-- = ", a--)
console.log("a = ", a)
console.log("a-- = ", a--)

x = 5
y = "6"
console.log("x == y is ", x == y) // equal to
console.log("x != y is ", x != y) // not equal
console.log("x === y is ", x === y) // equal value and equal type
console.log("x !== y is ", x !== y) // not equal value or not equal type
console.log("x < y is ", x < y) // Less than

// Logical Operators
if (2 < 5 && 1 > 0){ // ogical and
  console.log('True', 'AND')
}
if (2 < 5 || 1 == 0){ // logical or
  console.log('OR')
}
if (2 != 5){ // logical not
  console.log('NOT')
}

/* 

Operator 	Description
  && 	    logical and
  || 	    logical or
  ! 	    logical not
  
*/



let age = parseInt(console.log("Enter your age (for testing): ")); // Replace with actual user input later

if (age < 18) {
  text = "Too young to buy alcohol";
} else {
  text = "You are allowed to enter the club!";
}

console.log(text);







/*

Operator 	    Example 	    Same As
  = 	         x = y 	       x = y
  += 	         x += y 	   x = x + y
  -= 	         x -= y 	   x = x - y
  *= 	         x *= y 	   x = x * y
  /= 	         x /= y 	   x = x / y
  %= 	         x %= y 	   x = x % y
  **= 	       x **= y 	   x = x ** y

  */

/*
Operator 	    Description
  == 	        equal to
  === 	      equal value and equal type
  != 	        not equal
  !== 	      not equal value or not equal type
  > 	        greater than
  < 	        less than
  >= 	        greater than or equal to
  <= 	        less than or equal to
  ? 	        ternary operator

*/